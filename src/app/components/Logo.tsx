import LogoSvg from "../../../public/logo/logo.svg";

const Logo = () => (
	<LogoSvg
		style={{ filter: "drop-shadow(0 4px 12px rgba(0, 0, 0, 0.12))" }}
		aria-hidden="true"
	/>
);

export default Logo;
