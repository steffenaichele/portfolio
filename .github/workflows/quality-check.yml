name: Quality Check

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop, main]

jobs:
  build-and-check:
    name: Build & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_EMAIL: ${{ secrets.NEXT_PUBLIC_EMAIL }}

      - name: Check for build warnings
        run: |
          if grep -q "⚠" .next/trace; then
            echo "::warning::Build completed with warnings"
          fi

  accessibility-check:
    name: Accessibility Audit
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v44
        with:
          files: |
            src/**/*.tsx
            src/**/*.ts

      - name: Comment on PR
        if: steps.changed-files.outputs.any_changed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const changedFiles = '${{ steps.changed-files.outputs.all_changed_files }}'.split(' ');
            const fileList = changedFiles.map(f => `- \`${f}\``).join('\n');

            const body = `## 🔍 Accessibility Check

            Changed files to review:
            ${fileList}

            ### Checklist
            - [ ] Icon-only buttons have \`aria-label\`
            - [ ] Form inputs have labels
            - [ ] Images have \`alt\` text
            - [ ] No \`transition-all\` usage
            - [ ] \`prefers-reduced-motion\` respected
            - [ ] Focus states visible

            Run \`/pre-pr-light\` locally for detailed check.`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

  deploy-preview:
    name: Deploy Preview (Vercel)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Vercel (Preview)
        id: vercel-deploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          npm i -g vercel@latest
          vercel --version

          # Deploy and capture ONLY one URL (first match), remove any newlines
          PREVIEW_URL="$(vercel deploy --token="$VERCEL_TOKEN" --yes 2>&1 \
            | grep -Eo 'https://[^ ]+' \
            | head -n 1 \
            | tr -d '\r\n')"

          if [ -z "$PREVIEW_URL" ]; then
            echo "Failed to extract preview URL from Vercel output" >&2
            exit 1
          fi

          echo "preview_url=$PREVIEW_URL" >> "$GITHUB_OUTPUT"

      - name: Comment Preview URL
        if: steps.vercel-deploy.outputs.preview_url != ''
        uses: actions/github-script@v7
        with:
          script: |
            const url = '${{ steps.vercel-deploy.outputs.preview_url }}';
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🚀 Preview Deployment\n\n✅ Preview available at: ${url}`
            });
